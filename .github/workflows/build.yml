name: Build Windows Installer

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller --noconfirm --onefile --add-data "dashboard.py;." --collect-all streamlit --copy-metadata streamlit --icon "assets\icon.ico" --name "ITBudgetMedkvadrat" run_app.py

      - name: Compile Inno Setup Installer
        run: |
          iscc installer.iss

      - name: Upload Installer Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ITBudgetMedkvadratSetup
          path: Output\ITBudgetMedkvadratSetup.exe
